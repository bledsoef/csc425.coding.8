#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]) {
    int megs = argv[1];
    int* arrayPtr;
    arrayPtr = (int *)malloc(megs);

    while (1) {
        arrayPtr
    }

    free(arrayPtr);
}